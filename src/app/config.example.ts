/**
 * ⚙️ CONFIGURATION TEMPLATE
 *
 * Copy this file to `config.ts` (alongside it) and fill in your values.
 * The actual `config.ts` is gitignored so you don't leak your host/IDs.
 *
 *   cp src/app/config.example.ts src/app/config.ts
 *
 * Then edit src/app/config.ts with your real values.
 */

export const THOUGHTSPOT_HOST = "https://your-instance.thoughtspot.cloud";

export const LIVEBOARD_ID = "your-liveboard-id-here";
